git init
git add .
git commit -m "Arithmetic Operations"
git status
git push -u origin main

-- to link with github
git remote add origin git@github.com:gusperezprieto/TESTMODULE.git

-- to list dependecies
go list
go list -m all
go list all // show all packages available